#==============================================================================
# Dependências — CPU
#
# Cobre: src/preprocessing e src/models (supervisionados)
#
#   pip install -r requirements.txt
#==============================================================================

# Manipulação de dados
numpy>=1.24
pandas>=2.0

# Validação cruzada, pré-processamento, métricas
scikit-learn>=1.3

# Modelos supervisionados
xgboost>=2.0

# Paralelização do grid search (src/models/grid_search.py)
joblib>=1.3

# Barra de progresso na geração dos folds (src/preprocessing/cross_validation.py)
tqdm>=4.65

# Detecção de núcleos físicos (src/models/run_supervised_grid_search.py)
psutil>=5.9

# Figuras e testes estatísticos da análise exploratória (src/analysis/)
matplotlib>=3.8
scipy>=1.11

#==============================================================================
# Dependências — GPU (somente src/models/run_hdbscan_grid_search.py)
#
# NÃO instaláveis por este arquivo: exigem Linux + CUDA 12 e índices próprios.
# O experimento com HDBSCAN foi executado no Google Colab com GPU NVIDIA.
#
#   pip install --extra-index-url=https://pypi.nvidia.com cuml-cu12
#   pip install "git+https://github.com/FelSiq/DBCV"
#
# cuml-cu12   -> cuml.cluster.HDBSCAN e cuml.cluster.hdbscan.approximate_predict
# dbcv        -> métrica Density-Based Clustering Validation (critério do grid)
#==============================================================================

#==============================================================================
# Desenvolvimento
#
# Suite de testes unitarios (tests/), executada com:
#   pytest
#==============================================================================
pytest>=8.0

#==============================================================================
# Reprodutibilidade
#
# Para congelar as versões exatas usadas na execução final dos experimentos:
#   pip freeze > requirements-lock.txt
#==============================================================================
